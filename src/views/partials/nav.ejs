<header class="nav">
  <div class="row" style="gap:14px">
    <a class="brand" href="/"><%= appName %></a>
    <span class="muted" style="font-size:13px">Cars · Sales · Reports</span>
  </div>
  <nav class="navlinks">
    <a class="pill" href="/cars">Inventory</a>
    <a class="pill" href="/contact">Contact</a>
    <a class="pill" href="/admin">Admin</a>
    <% if (isAdmin) { %>
      <a class="pill" href="/admin/settings">Settings</a>
      <form method="post" action="/admin/logout" style="margin:0">
        <button class="pill" type="submit" style="cursor:pointer">Logout</button>
      </form>
    <% } else { %>
      <a class="pill" href="/admin/login">Login</a>
    <% } %>
  </nav>
</header>
